{
  "name": "haptic-browser",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:screenshot": "CI=true playwright test tests/screenshot.spec.ts --reporter=list --max-failures=1",
    "test:install": "playwright install firefox"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "playwright": "^1.56.1",
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "@types/three": "^0.181.0",
    "three": "^0.181.2"
  }
}
